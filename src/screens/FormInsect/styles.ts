import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'column',
    rowGap: 70,
  },
  image: {
    alignItems: 'center',
  },
  inputs: {
    flexDirection: 'column',
    rowGap: 14,
  },
});
